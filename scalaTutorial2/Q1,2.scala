/*
19001304
*/

object Calculation {
   def main(args: Array[String]) {
      //Q1,Q2
      var (k ,i, j) = (2,2,2);
      val (m ,n )= (5,5);
      var	f = 12.0f;
      var	g = 4.0f;
      var	c = 'X';
      
      println(k + 12 * m); 
      println(n % j); 
      println(m / j); 
      println(m / j * j);
      println(f + 10*5 +g);
      i+=1;
      println(i * n);
   }
}